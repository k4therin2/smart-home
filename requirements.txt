# Smart Home Assistant - Python Dependencies
# Phase 1: Foundation & Core Infrastructure

# LLM Integration
openai>=1.0.0

# Web Framework
flask>=3.0.0

# Home Assistant Integration
requests>=2.31.0

# Spotify Integration (Phase 2)
spotipy>=2.23.0

# Environment Configuration
python-dotenv>=1.0.0

# Database
# Using SQLite (built-in) for local storage

# Utilities
python-dateutil>=2.8.2
pytz>=2024.1

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
responses>=0.24.0

# Security (Phase 2.1 & 2.2)
flask-login>=0.6.3
flask-wtf>=1.2.1
flask-limiter>=3.5.0
pydantic>=2.5.0
argon2-cffi>=23.1.0
pyOpenSSL>=24.0.0

# API Documentation (WP-10.18)
flasgger>=0.9.7

# Metrics & Monitoring (WP-10.20)
prometheus-client>=0.19.0

# SSO Authentication (WP-90.5)
authlib>=1.3.0

# Object Detection (WP-11.1)
# IMPORTANT: Install CPU-only PyTorch FIRST to avoid 10GB+ CUDA layers
# See RCA: 2026-01-03-disk-space-sev0.md
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics>=8.0.0
Pillow>=10.0.0

# MQTT Integration (WP-10.28)
paho-mqtt>=2.0.0
